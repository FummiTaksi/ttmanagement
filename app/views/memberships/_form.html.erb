<%= form_for(current_player.club) do |f| %>
  <% if current_player.club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(current_player.club.errors.count, "error") %> prohibited this membership from being saved:</h2>

      <ul>
      <% current_player.club.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.select :id, options_from_collection_for_select(@players, :id, :to_s) %>
    <%= f.submit %>

<% end %>
