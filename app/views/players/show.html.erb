<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @player.firstname %>
  <%= @player.lastname %>
      </p>

<p>
  <Strong>Username:</Strong>
  <%=@player.username %>
</p>

<p>
  <strong>Birthday:</strong>
  <%= @player.birthday%>
</p>

<p>
  <strong>Age:</strong>
  <%= @player.count_age %>
</p>

<p></p>
<% if @player.club %>
<%= @player %> is member of <%= @player.club%>
    <% else %>
    <%=@player%> doesn't have a club.
<% end %>
<br>
<b> This player has played <%= @player.matches.size%> matches, won <%=@player.won_matches.size %> matches.
     win precentange <%= @player.win_precentage %> % </b>

<br></br>
<% if @player.clubowner %>

<p>
  <%=@player%> is clubowner
</p>
    <br></br>
    <% else %>
        <%=@player%> is not a clubowner

 <% end %>
<% if @player.admin %>

<p>
  <%=@player%> is admin!
</p>
    <% else %>
   <p><%=@player%> is not admin.</p>

    <% end %>



        <%=  link_to 'Toggle clubowner', toggle_clubowner_player_path(@player.id), class:"btn btn-success" , method: :post if current_player and current_player.admin %>

    <br></br>

        <%= link_to 'Toggle admin', toggle_admin_player_path(@player.id), class:'btn btn-primary' , method: :post if current_player and current_player.admin %>



<br><%=link_to 'Edit', edit_player_path(@player), class:'btn btn-primary' if current_player and current_player.username == @player.username %> </br>
<br><%= link_to 'Destroy', @player, method: :delete, data: { confirm: 'Are you sure?' }, class:'btn btn-danger' if current_player and current_player.username == @player.username%> </br>
<br><%= link_to 'Back', players_path, class:'btn btn-primary' %> </br>
